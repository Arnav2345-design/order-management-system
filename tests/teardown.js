// tests/teardown.js

module.exports = async () => {
  // Nothing to do here for now.
  // Jest will exit automatically after this runs.
  // We'll add pool/redis cleanup here if Jest hangs on exit.
};