// tests/setup.js

module.exports = async () => {
  // Override environment variables to point at the test database.
  // This must happen before any app code is required, because
  // database.js reads these variables when the Pool is created.
  
};